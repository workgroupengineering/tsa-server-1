tsa:
  certificate:
    path: "classpath:cert/rsa.p12"
  accepted-hash-algorithms:
    - SHA512
  # chose random ports outside the well-known port range
  server:
    port: 0
  http:
    port: 0
spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: "jdbc:h2:mem:test"
  sql:
    init:
      platform: h2
